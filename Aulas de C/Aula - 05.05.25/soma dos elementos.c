#include <stdio.h>
int main(){ 
    int n,soma=0;
        scanf("%d",&n);
    for(int i=1;i<=n;i++){
        int a;
            scanf("%d",&a);
        soma = soma + a;
    }
    printf("%d",soma);
  return 0;
}
// Exercício 159